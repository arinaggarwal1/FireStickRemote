{
  "name": "firetv-remote-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node server/index.js",
    "start": "NODE_ENV=production node server/index.js",
    "postinstall": "mkdir -p data public && cp -n sample.env .env 2>/dev/null || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "js-yaml": "^4.1.0"
  }
}
